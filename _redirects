# Netlify-Weiterleitungen
#
# Ökosystem: Die Website und das Anmeldesystem sind zwei getrennte
# Anwendungen, sollen sich für Besucher aber wie eine anfühlen. Deshalb
# liegt die Online-Anmeldung unter /anmeldung auf DIESER Domain und wird
# von hier auf die Anwendung weitergeleitet.
#
# ────────────────────────────────────────────────────────────────────
#  HIER DIE ECHTE ADRESSE EINTRAGEN, SOBALD DAS ANMELDESYSTEM
#  DEPLOYT IST. Bis dahin läuft der Button "Online Mitglied werden"
#  auf preise.html ins Leere.
#
#  Das Anmeldesystem liegt in /Users/gsebastianp/IMPACT-signup und
#  braucht einen eigenen Deploy (eigenes Netlify-Projekt oder Vercel),
#  weil es einen Server für Stripe-Webhooks und Secrets benötigt.
#  Es darf NICHT in dieses Repo — der Repo-Stamm wird öffentlich
#  ausgeliefert, der Server-Code wäre damit herunterladbar.
# ────────────────────────────────────────────────────────────────────

/anmeldung   https://ANMELDUNG-URL-HIER-EINTRAGEN.netlify.app/   302
/anmeldung/* https://ANMELDUNG-URL-HIER-EINTRAGEN.netlify.app/:splat   302
